<!DOCTYPE HTML>
<html lang="en-US">
<head>      
  <meta charset="UTF-8">
  <title>Shows</title>
  <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="bs-example">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Date</th>
          <th>Event</th>
          <th>Time</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% @concerts.each do | concert | %>
        <tr>
        <% if !concert.at_css('.sold-out, .custom').nil? %>
          <td><%= concert.at_css('.dates').text %></td>
          <td><del><%= concert.at_css('.headliners.summary').text %></del></td>
          <td><%= concert.at_css('.times').text %></td>
          <td> SOLD OUT </td>
        <% else %>
          <td><%= concert.at_css('.dates').text %></td>
          <td><a href="<%= concert.at_css('.ticket-link a')[:href] %>"><%= concert.at_css('.headliners.summary').text %></a></td>
          <td><%= concert.at_css('.times').text %></td>
          <td><%= concert.at_css('.price-range').text %></td>
        <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</body>
</html>